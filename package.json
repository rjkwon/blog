{
  "name": "kwon.nyc",
  "type": "module",
  "dependencies": {
    "googleapis": "^150.0.1"
  },
  "scripts": {
    "generate:cans": "node scripts/generate_cans_json.js",
    "generate:letterboxd": "node scripts/fetch_letterboxd.js",
    "generate:mastodon": "node scripts/fetch_mastodon.js",
    "prebuild": "npm run generate:cans && npm run generate:letterboxd && npm run generate:mastodon",
    "build": "npm run prebuild && hugo --gc"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.3.2"
  }
}
