{
  "name": "kwon.nyc",
  "type": "module",
  "dependencies": {
    "googleapis": "^150.0.1"
  },
  "scripts": {
    "generate:cans": "node scripts/generate_cans_json.js",
    "prebuild": "npm run generate:cans",
    "build": "npm run prebuild && hugo --gc --minify"
  }
}